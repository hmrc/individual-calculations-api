{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Retrieve a Self Assessment Tax Calculation Messages",
  "description": "Retrieve a Self Assessment Tax Calculation Messages",
  "type": "object",
  "properties": {
    "info": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A list of 'info' messages relating to the Calculation ID.",
        "properties": {
          "id": {
            "description": "The ID of the message.",
            "type": "string"
          },
          "text": {
            "description": "The message text.",
            "type": "string"
          },
          "additionalProperties": false
        },
        "required": [
          "id",
          "text"
        ]
      }
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A list of 'warning' messages relating to the Calculation ID.",
        "properties": {
          "id": {
            "description": "The ID of the message.",
            "type": "string"
          },
          "text": {
            "description": "The message text.",
            "type": "string"
          },
          "additionalProperties": false
        },
        "required": [
          "id",
          "text"
        ]
      }
    },
    "errors": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A list of 'error' messages relating to the Calculation ID.",
        "properties": {
          "id": {
            "description": "The ID of the message.",
            "type": "string"
          },
          "text": {
            "description": "The message text.",
            "type": "string"
          },
          "additionalProperties": false
        },
        "required": [
          "id",
          "text"
        ]
      }
    }
  }
}