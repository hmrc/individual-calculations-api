{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Trigger a Self Assessment Tax Calculation",
  "description": "Trigger a Self Assessment Tax Calculation",
  "type": "object",
  "properties": {
    "taxYear": {
      "type": "string",
      "description": "The tax year that the calculation is being triggered for. The start year and end year must not span two tax years. No gaps are allowed, for example, 2019-21 is not valid. The minimum tax year is 2017-18.",
      "example": "2019-20"
    }
  },
  "required": ["taxYear"]
}